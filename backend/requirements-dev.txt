# Development & testing dependencies.
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Load / stress testing
locust==2.32.5

# Unit-test tooling (Django's own test runner is used; these add coverage + fixtures)
coverage==7.6.9
